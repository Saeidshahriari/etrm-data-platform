# ==========================================================================
# Data Processing & Lakehouse
# PySpark is pinned to match the Spark 4.0.0 standalone cluster.
# ==========================================================================
pyspark==4.0.0
pandas>=2.1.0
pyarrow>=14.0.0

# Database & Storage
psycopg2-binary>=2.9.9
duckdb>=0.10.0

# Security (HashiCorp Vault)
hvac>=2.1.0

# API & Environment
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# ==========================================================================
# Data Quality & Poisoning Gates (Security Layer G)
# ==========================================================================
pydantic>=2.5.0
pandera>=0.20.0

# ==========================================================================
# MLOps - model, tracking and monitoring
# ==========================================================================
scikit-learn>=1.4.0
joblib>=1.3.0
mlflow>=2.14.0
evidently>=0.4.0

# ==========================================================================
# Dashboard (live view of the Gold layer)
# ==========================================================================
streamlit>=1.35.0
plotly>=5.20.0

# ==========================================================================
# AI Agent (MCP server over the lakehouse)
# ==========================================================================
mcp>=1.0.0

# ==========================================================================
# Orchestration provider
# ==========================================================================
apache-airflow-providers-apache-spark>=5.0.0

# ==========================================================================
# Testing & Security tooling (also runs in CI)
# ==========================================================================
pytest>=7.4.0
bandit>=1.7.0
pip-audit>=2.7.0

# Interactive Development
notebook>=7.0.0
